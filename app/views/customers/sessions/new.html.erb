<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
    <h4>会員の方はこちらからログイン</h4>
    <%= form_with model: @customer, url: session_path(resource_name), local: true do |f| %>
      <div class="form-group">
        <%= f.label "メールアドレス", for: "customer_email", class: "control-label" %>
        <%= f.text_field :email, class: "form-control" %>
      </div>
      <divc class="form-group">
        <%= f.label "パスワード", for: "customer_password", class: "control-label" %>
        <%= f.password_field :password, class: "form-control" %>
      </divc>
      <div class="submit-btn">
        <%= f.submit "ログイン", class: "btn btn-primary" %>
      </div>
    <% end %>
    <h4>登録がお済みでない方</h4>
    <p><%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください</p>
  </div>
</div>